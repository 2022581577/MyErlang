{
    application, server,
    [
        {description, "server"}
        ,{vsn, "1.0"}
        ,{modules, []}
        ,{registered, []}
        ,{applications, [kernel, sasl, stdlib, crypto, emysql]}
        ,{mod, {server_app, []}}
        ,{start_phases, []}
        ,{env, []}
    ]
}.
