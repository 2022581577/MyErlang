{
    application, server,
    [
        {description, "server"}
        ,{vsn, "1.0"}
        ,{modules, []}
        ,{registered, []}
        ,{applications, [kernel, sasl, stdlib]}
        ,{mod, {server_app, []}}
        ,{start_phases, []}
        ,{env, []}
    ]
}.
